<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yond.mapper.UserMapper">
    <sql id="ALL_FILED">
          id AS id,
          username AS username,
          password AS password,
          nickname AS nickname,
          avatar AS avatar,
          email AS email,
          create_time AS createTime,
          update_time AS updateTime,
          role AS role
    </sql>

    <!--按用户名查询User-->
    <select id="findByUsername" resultType="com.yond.entity.User">
        select
        <include refid="ALL_FILED"/>
        from user where username=#{username} limit 1
    </select>

    <!--按id查询User，实际上这个User就应该是博主-->
    <select id="findById" resultType="com.yond.entity.User">
        select
        <include refid="ALL_FILED"/>
        from user where id=#{id} limit 1
    </select>

    <!--按username修改-->
    <update id="updateUserByUsername">
        update user
        set username=#{user.username},
            password=#{user.password},
            update_time=now()
        where username = #{username}
    </update>
</mapper>
